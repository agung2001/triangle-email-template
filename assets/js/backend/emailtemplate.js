jQuery(document).ready(function( $ ) {

    /**
     *
     * @emailtemplate @edit
     * */
    // Load editor

    // Textarea
    // var textarea = $('#triangle-template-script-field').hide();
    // editor.getSession().setValue(textarea.val());
    // editor.getSession().on('change', function(){
    //     textarea.val(editor.getSession().getValue());
    // });

});